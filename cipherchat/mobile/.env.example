# OPTIONAL — normally you don't need a .env at all.
# When run via `npx expo start`, the app auto-detects the CipherChat server on
# the same machine (port 4000). Set this only to point at a server elsewhere:
# EXPO_PUBLIC_API_URL=https://your-deployed-server.example.com
